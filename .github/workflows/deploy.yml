<<<<<<< HEAD
name: 🚀 Deploy
=======
name: Manual Deploy
>>>>>>> feature/friction

on:
  workflow_dispatch:
    inputs:
<<<<<<< HEAD
      target:
        description: 'Target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      version:
        description: 'Version tag (e.g. 2.1.0) — leave blank to skip'
=======
      source_branch:
        description: 'Branch to deploy (merged → develop → main)'
>>>>>>> feature/friction
        required: false
        default: ''

jobs:
<<<<<<< HEAD
  deploy:
    runs-on: ubuntu-latest
    environment: production.momentum.joehunter.dev

=======
  merge-and-deploy:
    runs-on: ubuntu-latest
    environment: production.momentum.joehunter.dev
>>>>>>> feature/friction
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
<<<<<<< HEAD

      - name: Tag version
        if: ${{ github.event.inputs.version != '' }}
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag -a "v${{ github.event.inputs.version }}" -m "Release v${{ github.event.inputs.version }}"
          git push origin "v${{ github.event.inputs.version }}"
=======
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Merge feature branch → dev → main
        run: |
          SOURCE="${{ github.event.inputs.source_branch }}"

          # Ensure all remote branches are available locally
          git fetch --all

          # If a feature branch was specified, merge it into dev first
          if [ -n "$SOURCE" ] && [ "$SOURCE" != "develop" ] && [ "$SOURCE" != "main" ]; then
            echo "Merging $SOURCE → develop"
            git checkout -B develop origin/develop
            git merge --no-ff "origin/$SOURCE" -m "chore: merge $SOURCE into develop [deploy]"
            git push origin develop
          else
            git checkout -B develop origin/develop
          fi

          # Merge develop → main
          echo "Merging develop → main"
          git checkout -B main origin/main
          git merge --no-ff develop -m "chore: merge develop into main [deploy]"
          git push origin main
>>>>>>> feature/friction

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer:v2

      - name: Install Composer dependencies
        run: composer install --no-dev --no-interaction --optimize-autoloader

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Build frontend assets
        run: |
          npm ci
          npm run build

<<<<<<< HEAD
      - name: Deploy via FTP
=======
      - name: Deploy only changed files via FTP
>>>>>>> feature/friction
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: ${{ secrets.FTP_PORT }}
          protocol: ftp
          local-dir: ./
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
<<<<<<< HEAD
          state-name: .ftp-deploy-sync-state.${{ github.event.inputs.target }}.json
=======
          state-name: .ftp-deploy-sync-state.json
>>>>>>> feature/friction
          exclude: |
            .git/**
            .github/**
            .docs/**
            .vscode/**
            .idea/**
            .fleet/**
            **/.private/**
            node_modules/**
            vendor/**
            tests/**
            storage/logs/**
            storage/framework/cache/**
            storage/framework/sessions/**
            storage/framework/testing/**
            storage/framework/views/**
            storage/debugbar/**
            bootstrap/ssr/**
            .env*
            *.log
            npm-debug.log
            yarn-error.log
            .phpunit.cache
            .phpunit.result.cache
            phpunit.xml
            README.md
            TODO.md
            *.code-workspace

<<<<<<< HEAD
      - name: Laravel optimize (SSH)
=======
      - name: Composer install and Laravel optimize
>>>>>>> feature/friction
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            cd ${{ secrets.SSH_DEPLOY_PATH }}
            /opt/alt/php84/usr/bin/php /usr/local/bin/composer install --no-dev --no-interaction --optimize-autoloader
            /opt/alt/php84/usr/bin/php artisan config:cache
            /opt/alt/php84/usr/bin/php artisan route:cache
            /opt/alt/php84/usr/bin/php artisan view:cache
            /opt/alt/php84/usr/bin/php artisan optimize
