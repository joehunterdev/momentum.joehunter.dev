so the aim now is lets create a feature for the rows, even if you want to build a standalone component if it helps, the core feature that now i want it to be richher feedback when actioning the item

so dragging the icon left with thumb could have some varying level of required input to drag or reach the end of the row to complete the action. This input will be based on a balance factor
so each if success rate is 70 make it so the drag requires to move 30% right. Maby enforce some kind of easing or frinction on the drag action to re enforce this ? what do u think please write p